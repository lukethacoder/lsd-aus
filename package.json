{
  "license": "MIT",
  "scripts": {
    "bs": "lerna bootstrap",
    "postinstall": "yarn bootstrap",
    "publish": "lerna publish",
    "test": "lerna run test",
    "lint": "eslint packages"
  },
  "devDependencies": {
    "@lsd/eslint-config": "file:packages/eslint-config",
    "@lsd/prettier-config": "file:packages/prettier-config",
    "eslint": "6.8.0",
    "lerna": "3.20.2"
  }
}
